# 路由與導覽

## 路由設定

每個 H5 應用的路由定義在 `src/router/` 目錄下。

## 版面配置

`BasicLayout` 元件包含：

- **頂部導覽列**：顯示當前頁面標題，非 Tab 頁面顯示返回按鈕
- **底部 TabBar**：首頁 / 列表 / 我的 / 範例
- **主內容區**：有 TabBar 時自動增加底部內邊距避免遮擋

## 動態標題

通過 `@vueuse/core` 的 `useTitle` 實現頁面標題自動跟隨路由切換。
