PORT=5320
MOCK_ALLOWED_ORIGINS=http://localhost:5777,http://localhost:5778,http://localhost:5779

# Required if this mock server is ever run with NODE_ENV=production.
# Use independent, high-entropy values and never expose them through VITE_*.
ACCESS_TOKEN_SECRET=replace-with-a-long-random-access-secret
REFRESH_TOKEN_SECRET=replace-with-a-long-random-refresh-secret
